#!/usr/bin/env python3

import prn
import rnd

# ---------------------------------------------------------------------------- #
a = rnd.get_rnd(32)
prn.print_bytes(a, 24)

# ---------------------------------------------------------------------------- #
a = rnd.get_rnd(1343)
prn.print_bytes(a, 16)

# ---------------------------------------------------------------------------- #
a = rnd.get_rnd(332)
prn.print_bytes(a, 8)

# ---------------------------------------------------------------------------- #
a = rnd.get_rnd(1011)
prn.print_bytes(a, 1)

# ---------------------------------------------------------------------------- #
a = rnd.get_rnd(100)
prn.print_bytes(a, 2)

# ---------------------------------------------------------------------------- #
a = rnd.get_rnd(100)
prn.print_bytes(a, 25)

# ---------------------------------------------------------------------------- #
a = rnd.get_rnd(1023)
prn.print_bytes(a, 25)

